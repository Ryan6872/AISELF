# 别再造轮子了！30秒通过 API 分析 Python 代码复杂度 (免费)

> **摘要**：还在为项目写复杂的 AST 解析器吗？本文介绍一个名为 Prometheus 的免费 API，哪怕你是编程小白，也能在 30 秒内获得代码的“体检报告”。

---

作为一名 Python 开发者，我们经常需要回答这几个问题：
*   *"我写的这个函数是不是太复杂了？"*
*   *"这个脚本里到底定义了多少个类？"*
*   *"这段文本读完需要几秒钟？"*

通常，你需要引入 `ast` 模块，写一堆递归函数来遍历语法树…… **太麻烦了！**

今天我向大家推荐一个刚刚上线的神器 —— **Prometheus Text & Code Toolkit**。

## 它是干嘛的？
它是一个部署在云端的无服务器 API，专门用来做两件事：
1.  **代码体检**：瞬间分析 Python 代码结构，计算圈复杂度 (Cyclomatic Complexity)。
2.  **文本工具**：提取关键词、计算阅读时间、生成 URL Slug。

最重要的是：**它对个人开发者完全免费！** (每月 500 次调用额度)

## 怎么用？

### 1. 获取密钥
去 [RapidAPI 订阅页面](https://rapidapi.com/liaoyingg/api/prometheus-text-and-code-toolkit) 点击 **Subscribe (Basic Plan)**，完全免费，不需要付费。

### 2. 抄代码
拿到 API Key 后，直接复制下面的代码：

```python
import requests

url = "https://prometheus-text-and-code-toolkit.p.rapidapi.com/api/explain"

# 你想要分析的代码
my_code = """
def hello_world():
    print('Hello')
    if True:
        print('Complex!')
"""

payload = {
	"code": my_code,
	"language": "python"
}

headers = {
	"x-rapidapi-key": "你的_API_KEY",
	"x-rapidapi-host": "prometheus-text-and-code-toolkit.p.rapidapi.com",
	"Content-Type": "application/json"
}

response = requests.post(url, json=payload, headers=headers)

# 打印分析结果
print(response.json())
```

### 3. 看结果
你会得到这样的 JSON，清晰明了：
```json
{
  "functions": [
    {
      "name": "hello_world",
      "lineno": 1,
      "args": []
    }
  ],
  "complexity": 2, 
  "classes": [],
  "imports": []
}
```

## 为什么选择它？
*   **隐私安全**：它只做静态分析，**不存储**你的任何代码。
*   **极速**：基于优化的 AST 算法，不像大模型那样慢吞吞，结果是毫秒级的。
*   **省心**：不用维护本地环境，由 Vercel 强力驱动。

👉 **立即体验**：[点击这里跳转 RapidAPI](https://rapidapi.com/liaoyingg/api/prometheus-text-and-code-toolkit)

---
*如果你觉得好用，别忘了在 RapidAPI 上给我点个好评！*
